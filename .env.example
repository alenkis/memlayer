# Copy to .env and fill in your API keys:
#   cp .env.example .env
#
# Non-secret settings (ports, paths, flags) are in resources/config.edn.

OPENAI_API_KEY=your-openai-api-key
GROQ_API_KEY=your-groq-api-key
